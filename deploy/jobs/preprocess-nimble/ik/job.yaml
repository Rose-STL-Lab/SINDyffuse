# Worker phase: kubectl apply -k deploy/jobs/preprocess-nimble/ik
apiVersion: batch/v1
kind: Job
metadata:
  name: sindyffuse-preprocess-ik
  labels:
    app: sindyffuse-preprocess-ik
    activation-method: ik
spec:
  parallelism: 64
  completions: 64
  completionMode: Indexed
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: sindyffuse-preprocess-ik
        activation-method: ik
    spec:
      restartPolicy: Never
      terminationGracePeriodSeconds: 300
      containers:
      - name: sindyffuse-preprocess-ik
        image: sindyffuse:latest
        imagePullPolicy: IfNotPresent
        workingDir: /mnt/SINDyffuse
        env:
        - name: PREPROCESS_NUM_SHARDS
          value: "64"
        - name: MAX_MOTIONS
          value: "0"
        - name: SKIP_EXISTING
          value: "1"
        - name: SINDYFFUSE_RUN_LOG_ID
          value: "RUN_LOG_ID_PLACEHOLDER"
        command: ["/bin/bash", "-c"]
        args:
        - |
          source deploy/scripts/job-env.sh
          python scripts/preprocess_ik.py --skip_normalization --num_workers 1
        resources:
          limits:
            memory: 2Gi
            cpu: "1"
          requests:
            memory: 2Gi
            cpu: "1"
        volumeMounts:
        - name: data
          mountPath: /mnt
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: sindyffuse
