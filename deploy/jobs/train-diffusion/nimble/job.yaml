# Apply: kubectl apply -k deploy/jobs/train-diffusion/nimble
apiVersion: batch/v1
kind: Job
metadata:
  name: sindyffuse-diffusion-train-nimble
  labels:
    app: sindyffuse-diffusion-train-nimble
    guidance: nimble
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: sindyffuse-diffusion-train-nimble
        guidance: nimble
    spec:
      restartPolicy: Never
      containers:
      - name: sindyffuse-diffusion
        image: sindyffuse:latest
        imagePullPolicy: IfNotPresent
        workingDir: /mnt/SINDyffuse
        env:
        - name: NPROC_PER_NODE
          value: "2"
        command: ["/bin/bash", "-c"]
        args:
        - |
          set -eo pipefail
          eval "$(conda shell.bash hook)"
          conda activate sindyffuse
          export LD_LIBRARY_PATH="${CONDA_PREFIX}/lib:${LD_LIBRARY_PATH:-}"
          export PYTHONPATH=/mnt/SINDyffuse
          export PYTORCH_CUDA_ALLOC_CONF=expandable_segments:True
          export OMP_NUM_THREADS=1 MKL_NUM_THREADS=1 OPENBLAS_NUM_THREADS=1
          HML=/mnt/SINDyffuse/datasets/HumanML3D
          test -d "${HML}/nimble_b3d" || { echo "Missing ${HML}/nimble_b3d"; exit 1; }
          RUN=/mnt/SINDyffuse/results/diffusion_nimble_$(date +%Y%m%d_%H%M%S)
          mkdir -p "${RUN}"
          CFG="${RUN}/config_resolved.json"
          python - <<PY
          import json
          from pathlib import Path
          repo = Path("/mnt/SINDyffuse")
          cfg = json.loads((repo / "configs/train_diffusion.json").read_text(encoding="utf-8"))
          cfg.setdefault("train", {})["guidance"] = "nimble"
          cfg.setdefault("data", {})["data_root"] = str(repo / "datasets/HumanML3D")
          cfg.setdefault("data", {})["dataset"] = "nimble"
          Path("${RUN}/config_resolved.json").write_text(json.dumps(cfg, indent=2), encoding="utf-8")
          PY
          python scripts/train_diffusion.py \
            --config "${CFG}" \
            --out_dir "${RUN}" \
            --preload
        resources:
          limits:
            nvidia.com/gpu: 2
            memory: 64Gi
            cpu: "16"
          requests:
            nvidia.com/gpu: 2
            memory: 64Gi
            cpu: "16"
        volumeMounts:
        - name: data
          mountPath: /mnt
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: sindyffuse
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 64Gi
