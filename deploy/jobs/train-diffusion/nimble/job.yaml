# Apply: kubectl apply -k deploy/jobs/train-diffusion/nimble
apiVersion: batch/v1
kind: Job
metadata:
  name: sindyffuse-diffusion-train-nimble
  labels:
    app: sindyffuse-diffusion-train-nimble
    guidance: nimble
spec:
  backoffLimit: 0
  template:
    metadata:
      labels:
        app: sindyffuse-diffusion-train-nimble
        guidance: nimble
    spec:
      restartPolicy: Never
      containers:
      - name: sindyffuse-diffusion
        image: sindyffuse:latest
        imagePullPolicy: IfNotPresent
        workingDir: /mnt/SINDyffuse
        env:
        - name: NPROC_PER_NODE
          value: "2"
        command: ["/bin/bash", "-c"]
        args:
        - |
          source deploy/scripts/job-env.sh
          python scripts/train_diffusion.py --guidance nimble --preload
        resources:
          limits:
            nvidia.com/gpu: 2
            memory: 64Gi
            cpu: "16"
          requests:
            nvidia.com/gpu: 2
            memory: 64Gi
            cpu: "16"
        volumeMounts:
        - name: data
          mountPath: /mnt
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: sindyffuse
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 64Gi
