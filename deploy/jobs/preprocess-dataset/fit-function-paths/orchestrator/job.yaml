# Apply: kubectl apply -k deploy/jobs/preprocess-dataset/fit-function-paths/orchestrator
# Submits prepare → convert → fit Jobs in order (kubectl wait; no sleep).
apiVersion: batch/v1
kind: Job
metadata:
  name: sindyffuse-path-fit-orchestrator
  labels:
    app: sindyffuse-path-fit-orchestrator
spec:
  parallelism: 1
  completions: 1
  backoffLimit: 1
  template:
    metadata:
      labels:
        app: sindyffuse-path-fit-orchestrator
    spec:
      restartPolicy: Never
      serviceAccountName: sindyffuse-preprocess-orchestrator
      containers:
      - name: orchestrator
        image: bitnami/kubectl:latest
        imagePullPolicy: IfNotPresent
        env:
        - name: SINDYFFUSE_ROOT
          value: /mnt/SINDyffuse
        command: ["/bin/bash", "/mnt/SINDyffuse/deploy/scripts/path-fit-orchestrate.sh"]
        resources:
          limits:
            memory: 2Gi
            cpu: "1"
          requests:
            memory: 2Gi
            cpu: "1"
        volumeMounts:
        - name: data
          mountPath: /mnt
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: sindyffuse
