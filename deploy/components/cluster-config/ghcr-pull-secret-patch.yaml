# Requires ghcr-secret in the target namespace (see deploy/README.md).
apiVersion: batch/v1
kind: Job
metadata:
  name: ghcr-pull-secret-patch
spec:
  template:
    spec:
      imagePullSecrets:
        - name: ghcr-secret
