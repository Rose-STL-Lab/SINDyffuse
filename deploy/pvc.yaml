# PVC for SINDyffuse data (repo checkout, HumanML3D, results, logs).
#
# Apply once per namespace:
#   kubectl apply -f deploy/pvc.yaml -n YOUR_NAMESPACE
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: sindyffuse
spec:
  storageClassName: rook-cephfs
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
